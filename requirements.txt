# CalcAny 数据分析可视化工具 - 依赖包列表
# 安装命令: pip install -r requirements.txt

# 核心依赖
# pandas>=1.5.0        # 已替换为轻量化数据处理模块
psutil>=5.9.0           # 系统监控和内存优化
tksheet>=7.0.0          # 高级表格组件，支持复制、选择等功能

# GUI框架 (Python标准库)
# tkinter - 通常包含在Python标准库中，无需单独安装

# 开发和打包工具 (可选)
# pyinstaller>=6.0.0   # 用于打包可执行文件
# setuptools>=65.0.0   # 用于项目构建

# 项目特性:
# - 模块化架构设计，包含utils.py通用工具模块
# - 支持数据验证、剪贴板操作、日志管理等功能
# - 经过代码重构优化，消除重复代码

# 注意事项:
# 1. 建议使用Python 3.8+版本以获得最佳兼容性
# 2. tkinter通常包含在Python标准库中，无需单独安装
# 3. 如果需要打包可执行文件，请取消注释pyinstaller
# 4. 所有工具函数已模块化，提高代码可维护性